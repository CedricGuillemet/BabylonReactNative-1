/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from 'playground-shared';
import {name as appName} from 'playground-shared/app.json';

AppRegistry.registerComponent(appName, () => App);
